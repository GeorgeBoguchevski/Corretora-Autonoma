import './Header.css';
import React from 'react';

const Header = (props) => (
    <header className="header d-flex align-items-center">
        <h1 className='me-3'>
        </h1>
    </header>
);

export default Header;
